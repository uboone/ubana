<!-- Project details -->
<!DOCTYPE project [
<!ENTITY release "v08_00_00_17">
<!ENTITY name "flattree">
<!ENTITY user "kduffy">
<!ENTITY version "v08_00_00_17">
]>

<project name="&name;">

  <!-- Group -->
  <group>uboone</group>

  <!-- Project size (numevents is an upper limit) -->
  <numevents>50000</numevents>
  <numjobs>1000</numjobs>

  <!-- Where to look for fcl files (in addition to $FHICL_FILE_PATH) -->
  <!-- fcldir>/pnfs/uboone/persistent/users/ddevitt/fcl/</fcldir -->

  <!-- Batch OS -->
  <os>SL6</os>

  <!-- Larsoft information -->
  <larsoft>
    <!-- tag>&release;</tag -->
    <!-- qual>e10:prof</qual -->
    <local>/pnfs/uboone/resilient/users/kduffy/tars/flattreemaker.tar</local>
  </larsoft>

  <!-- Stages -->
  <stage name = "FlatTree">
     <maxfilesperjob>2</maxfilesperjob>
     <inputdef>prodgenie_bnb_nu_uboone_overlay_det_var_run1_mcc9.1_cv_reco2</inputdef>
     <fcl>run_FlatTreeMakerPDStyle.fcl</fcl>
     <outdir>/pnfs/uboone/persistent/users/kduffy/CC1pi/GeantRW/&version;/</outdir>
     <logdir>/pnfs/uboone/persistent/users/kduffy/CC1pi/GeantRW/&version;/</logdir>
     <workdir>/pnfs/uboone/resilient/users/kduffy/CC1pi/GeantRW/work/&version;/</workdir>
     <bookdir>/uboone/data/users/kduffy/CC1pi/GeantRW/book/&version;/</bookdir>
     <schema>root</schema>
     <!-- requested memory, in MB -->
     <memory>5000</memory>
  </stage>
</project>
